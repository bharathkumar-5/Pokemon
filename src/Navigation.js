import react from "react";
const [navigate,setnavigate] = usestate("")

const navi() => {
    const navigate()=>{
        usestate("")
    }
    return(
        <button onChange={navigate}><button/>
    )
}
export default navi;